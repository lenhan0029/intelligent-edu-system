server:
  port: 8088

spring:
  application:
    name: file-service
  datasource:
    url: jdbc:postgresql://localhost:5432/file_service_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
